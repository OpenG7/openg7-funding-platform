services:
  traefik:
    image: traefik:v3.2
    command:
      - --configFile=/etc/traefik/traefik.yml
    env_file:
      - .env
    ports:
      - '80:80'
      - '443:443'
      - '443:443/udp'
      - '${TRAEFIK_DASHBOARD_BIND:-127.0.0.1:8081}:8080'
    volumes:
      - ./traefik/traefik.yml:/etc/traefik/traefik.yml:ro
      - ./traefik/dynamic.yml:/etc/traefik/dynamic.yml:ro
      - ./traefik/acme:/letsencrypt
    networks:
      - edge
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    logging:
      driver: json-file
      options:
        max-size: '10m'
        max-file: '5'
    healthcheck:
      test: ['CMD', 'traefik', 'healthcheck', '--ping']
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s

  api:
    image: ${API_IMAGE:-openg7-funding-api:local}
    build:
      context: .
      dockerfile: apps/funding-api/Dockerfile
    env_file:
      - .env
    environment:
      FUNDING_PLATFORM_ENV: production
      FUNDING_API_PORT: 3333
      APP_DOMAIN: ${APP_DOMAIN:-vps-ea750a98.vps.ovh.ca}
      FUNDING_PUBLIC_BASE_URL: ${FUNDING_PUBLIC_BASE_URL:-https://vps-ea750a98.vps.ovh.ca}
      FUNDING_ALLOWED_ORIGINS: ${FUNDING_ALLOWED_ORIGINS:-https://vps-ea750a98.vps.ovh.ca}
      FUNDING_ALLOWED_AMOUNTS: ${FUNDING_ALLOWED_AMOUNTS:-5,10,25,50,100}
    expose:
      - '3333'
    networks:
      - edge
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    logging:
      driver: json-file
      options:
        max-size: '10m'
        max-file: '5'
    healthcheck:
      test:
        [
          'CMD-SHELL',
          'node -e "fetch(''http://127.0.0.1:3333/health'').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"'
        ]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s

  web:
    image: ${WEB_IMAGE:-openg7-funding-web:local}
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      api:
        condition: service_healthy
      traefik:
        condition: service_healthy
    expose:
      - '8080'
    networks:
      - edge
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    logging:
      driver: json-file
      options:
        max-size: '10m'
        max-file: '5'
    healthcheck:
      test: ['CMD-SHELL', 'wget -qO- http://127.0.0.1:8080/health | grep -q ok']
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s

  cadvisor:
    image: gcr.io/cadvisor/cadvisor:v0.49.1
    ports:
      - '${CADVISOR_BIND:-127.0.0.1:8082}:8080'
    volumes:
      - /:/rootfs:ro
      - /var/run:/var/run:ro
      - /sys:/sys:ro
      - /var/lib/docker/:/var/lib/docker:ro
    networks:
      - edge
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    logging:
      driver: json-file
      options:
        max-size: '10m'
        max-file: '3'

networks:
  edge:
    name: openg7-edge
    driver: bridge
